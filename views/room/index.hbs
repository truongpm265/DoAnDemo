<h1 class="container text-center">Room List</h1>
<div class="container mb-2">
   <a class="btn btn-success container " href="/room/add">Add new Room</a>
   {{!-- <a class="btn green" href="/product/sort/asc">Name ascending</a>
   <a class="btn lime" href="/product/sort/desc">Name descending</a> --}}
</div>
{{!-- <div class="row">
   <form class="col m5" action="/product/search" method="post">
      <input class="form-control" type="search" name="keyword"
      placeholder="Search by product name" required>
   </form>
</div> --}}
<table class="table table-primary">
   <tr>
      <th>Room Number</th>
      <th>Room Name</th>
      <th>Type</th>
      <th>Price</th>
      <th>Image</th>
      <th>Description</th>
      <th>Availability</th>
      <th>Menu</th>
   </tr>
   {{#each roomList }}
   <tr>
      <td>{{ roomNumber }}</td>
      <td>{{ name }}</td>
      <td>{{ typeRoom.name }}</td>
      <td>{{ pricePerNight }}VNĐ</td>
      <td>
         <img src="{{ image }}" width="100" height="100">
      </td>
      <td>{{ description }}</td>
      <td>{{#if availability}}Available{{else}}Unavailable{{/if}}</td>
      <td>
         <a class="btn orange" href="/room/edit/{{ _id }}">Edit</a>
         <a class="btn brown" href="/room/delete/{{_id}}"
            onclick="return confirm('Do you want to delete this room ?')" ;>Delete</a>
      </td>
   </tr>
   {{/each}}
</table>